{
  "name": "@hananan/express-response-formatter",
  "version": "1.0.0",
  "dependencies": {
    "express": "^5.1.0",
    "zod": "^3.25.67"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "start": "node dist/index.js",
    "dev": "nodemon ./src/index.ts | npx pino-pretty"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "typescript": "^5.8.3"
  }
}
